# 设计模式

> 设计模式（Design Patterns）是软件工程中面向对象设计的经验总结，是一套被反复使用、多数人知晓的、经过分类编目的、代码设计经验的总结。使用设计模式是为了可重用代码、让代码更容易被他人理解、保证代码可靠性。

## 一、设计模式概述

### 1.1 什么是设计模式

设计模式不是一种具体的"技术"，而是一种思想、一种解决问题的方法论。它描述了在特定环境中反复出现的问题及其解决方案的核心精髓，使人们可以更加方便地复用成功的设计方案。

### 1.2 设计模式的四大要素

| 要素 | 说明 |
| --- | --- |
| **模式名称** | 助记词，用一两个词描述模式的问题、解决方案和效果 |
| **问题** | 描述何时使用该模式，解释设计问题和前提条件 |
| **解决方案** | 描述设计的组成成分、它们之间的相互关系及各自的职责 |
| **效果** | 描述模式应用的效果及权衡 |

### 1.3 设计模式的六大原则

1. **单一职责原则（SRP）**：一个类只负责一个功能领域中的相应职责。
2. **开闭原则（OCP）**：对扩展开放，对修改关闭。
3. **里氏替换原则（LSP）**：所有引用基类的地方必须能透明地使用其子类对象。
4. **依赖倒置原则（DIP）**：依赖于抽象，不依赖于具体实现。
5. **接口隔离原则（ISP）**：客户端不应该依赖它不需要的接口。
6. **迪米特法则（LoD）**：一个对象应该对其他对象保持最少的了解。

### 1.4 设计模式分类

根据 GoF（Gang of Four）的划分，23 种设计模式分为三大类：

```
设计模式
├── 创建型模式（5 种）—— 关注对象的创建过程
│   ├── 单例模式（Singleton）
│   ├── 工厂方法模式（Factory Method）
│   ├── 抽象工厂模式（Abstract Factory）
│   ├── 建造者模式（Builder）
│   └── 原型模式（Prototype）
│
├── 结构型模式（7 种）—— 关注类与对象的组合
│   ├── 适配器模式（Adapter）
│   ├── 桥接模式（Bridge）
│   ├── 组合模式（Composite）
│   ├── 装饰器模式（Decorator）
│   ├── 外观模式（Facade）
│   ├── 享元模式（Flyweight）
│   └── 代理模式（Proxy）
│
└── 行为型模式（11 种）—— 关注对象之间的通信
    ├── 责任链模式（Chain of Responsibility）
    ├── 命令模式（Command）
    ├── 解释器模式（Interpreter）
    ├── 迭代器模式（Iterator）
    ├── 中介者模式（Mediator）
    ├── 备忘录模式（Memento）
    ├── 观察者模式（Observer）
    ├── 状态模式（State）
    ├── 策略模式（Strategy）
    ├── 模板方法模式（Template Method）
    └── 访问者模式（Visitor）
```

---

## 二、创建型模式

创建型模式关注"如何创建对象"，将对象的创建与使用分离，隐藏创建逻辑，使代码更灵活。

### 2.1 单例模式（Singleton）

**意图**：确保一个类只有一个实例，并提供一个全局访问点。

**适用场景**：日志、配置、线程池、数据库连接池等需要全局唯一实例的场景。

**实现要点**：
- 构造函数私有化
- 提供静态访问方法
- 保证线程安全

```java
public class Singleton {
    // volatile 防止指令重排序
    private static volatile Singleton instance;

    private Singleton() {}

    public static Singleton getInstance() {
        if (instance == null) {                    // 第一次检查
            synchronized (Singleton.class) {
                if (instance == null) {            // 第二次检查
                    instance = new Singleton();
                }
            }
        }
        return instance;
    }
}
```

> **双重检查锁定（DCL）** 是最经典的单例写法，兼顾了懒加载与线程安全。

### 2.2 工厂方法模式（Factory Method）

**意图**：定义一个创建对象的接口，让子类决定实例化哪一个类，使一个类的实例化延迟到其子类。

**适用场景**：客户端不关心对象创建细节，只知道对应的"工厂"即可得到对象。

```java
// 抽象产品
interface Product {
    void use();
}

// 具体产品
class ConcreteProductA implements Product {
    public void use() { System.out.println("使用产品 A"); }
}

// 抽象工厂
interface Factory {
    Product create();
}

// 具体工厂
class ConcreteFactoryA implements Factory {
    public Product create() { return new ConcreteProductA(); }
}
```

### 2.3 抽象工厂模式（Abstract Factory）

**意图**：提供一个创建一系列相关或相互依赖对象的接口，而无需指定它们具体的类。

**与工厂方法的区别**：抽象工厂生产"产品族"（多个相关产品），工厂方法生产单一产品。

**适用场景**：跨平台 UI 组件（Windows/Mac 的 Button、Checkbox 等）、数据库多类型连接。

```java
interface GUIFactory {
    Button createButton();
    Checkbox createCheckbox();
}

class WindowsFactory implements GUIFactory {
    public Button createButton()   { return new WindowsButton(); }
    public Checkbox createCheckbox(){ return new WindowsCheckbox(); }
}

class MacFactory implements GUIFactory {
    public Button createButton()   { return new MacButton(); }
    public Checkbox createCheckbox(){ return new MacCheckbox(); }
}
```

### 2.4 建造者模式（Builder）

**意图**：将一个复杂对象的构建与它的表示分离，使同样的构建过程可以创建不同的表示。

**适用场景**：复杂对象（含多个可选参数）的构建，例如 SQL 查询语句、HTTP 请求、配置对象。

```java
public class HttpRequest {
    private final String method;
    private final String url;
    private final Map<String, String> headers;
    private final String body;

    private HttpRequest(Builder b) {
        this.method = b.method;
        this.url = b.url;
        this.headers = b.headers;
        this.body = b.body;
    }

    public static class Builder {
        private String method = "GET";
        private String url;
        private Map<String, String> headers = new HashMap<>();
        private String body;

        public Builder url(String url)               { this.url = url; return this; }
        public Builder method(String method)         { this.method = method; return this; }
        public Builder header(String k, String v)    { headers.put(k, v); return this; }
        public Builder body(String body)             { this.body = body; return this; }

        public HttpRequest build() { return new HttpRequest(this); }
    }
}
```

### 2.5 原型模式（Prototype）

**意图**：用原型实例指定创建对象的种类，并通过拷贝这些原型来创建新对象。

**适用场景**：创建成本较大的对象（如复杂初始化、深拷贝）需要快速生成副本。

```java
public class Prototype implements Cloneable {
    private String name;
    private List<String> tags;

    @Override
    protected Prototype clone() throws CloneNotSupportedException {
        Prototype clone = (Prototype) super.clone();
        clone.tags = new ArrayList<>(this.tags);  // 深拷贝
        return clone;
    }
}
```

---

## 三、结构型模式

结构型模式关注"如何组合类与对象"形成更大的结构，处理不同对象间的组合关系。

### 3.1 适配器模式（Adapter）

**意图**：将一个类的接口转换成客户希望的另一个接口，使原本由于接口不兼容而不能一起工作的类可以一起工作。

**类比**：电源适配器（220V → 5V）、Type-C 转 USB。

```java
// 目标接口（客户端期望的）
interface Target {
    void request();
}

// 被适配者（已有的、接口不兼容的）
class Adaptee {
    public void specificRequest() { System.out.println("特殊请求"); }
}

// 适配器
class Adapter implements Target {
    private Adaptee adaptee;
    public Adapter(Adaptee adaptee) { this.adaptee = adaptee; }
    public void request() { adaptee.specificRequest(); }
}
```

### 3.2 桥接模式（Bridge）

**意图**：将抽象部分与它的实现部分分离，使它们都可以独立地变化。

**适用场景**：多维变化（如"形状×颜色"），避免类爆炸（CircleRed、CircleBlue、SquareRed……）。

```java
interface Color {
    String fill();
}

class Red implements Color {
    public String fill() { return "红色"; }
}

abstract class Shape {
    protected Color color;
    public Shape(Color color) { this.color = color; }
    abstract void draw();
}

class Circle extends Shape {
    public Circle(Color color) { super(color); }
    void draw() { System.out.println("绘制" + color.fill() + "圆形"); }
}
```

### 3.3 组合模式（Composite）

**意图**：将对象组合成树形结构以表示"部分-整体"的层次结构，使客户端可以统一地处理单个对象和组合对象。

**适用场景**：文件系统（文件/文件夹）、组织架构、UI 树。

```java
interface Component {
    void operation();
}

class Leaf implements Component {
    public void operation() { System.out.println("叶子节点操作"); }
}

class Composite implements Component {
    private List<Component> children = new ArrayList<>();
    public void add(Component c)   { children.add(c); }
    public void remove(Component c){ children.remove(c); }
    public void operation() {
        for (Component c : children) c.operation();
    }
}
```

### 3.4 装饰器模式（Decorator）

**意图**：动态地给一个对象添加额外的职责，比生成子类（继承）更加灵活。

**适用场景**：Java I/O 流（`BufferedInputStream` 包装 `FileInputStream`）、订单价格叠加。

```java
interface Coffee {
    double cost();
}

class SimpleCoffee implements Coffee {
    public double cost() { return 10; }
}

class MilkDecorator implements Coffee {
    private Coffee coffee;
    public MilkDecorator(Coffee c) { this.coffee = c; }
    public double cost() { return coffee.cost() + 2; }
}

class SugarDecorator implements Coffee {
    private Coffee coffee;
    public SugarDecorator(Coffee c) { this.coffee = c; }
    public double cost() { return coffee.cost() + 1; }
}
```

### 3.5 外观模式（Facade）

**意图**：为子系统中的一组接口提供一个一致的界面，定义一个高层接口，使子系统更易使用。

**类比**：电脑的"开机键"——一键启动 CPU、内存、硬盘，而不需要用户逐个调用。

```java
class CPU       { void startup()  { System.out.println("CPU 启动"); } }
class Memory    { void startup()  { System.out.println("内存加载"); } }
class Disk      { void startup()  { System.out.println("硬盘读取"); } }

class Computer {
    private CPU cpu = new CPU();
    private Memory memory = new Memory();
    private Disk disk = new Disk();

    public void startup() {
        cpu.startup();
        memory.startup();
        disk.startup();
    }
}
```

### 3.6 享元模式（Flyweight）

**意图**：运用共享技术有效地支持大量细粒度的对象，减少内存占用。

**适用场景**：线程池、字符串常量池、棋盘上的棋子（颜色 + 位置）。

```java
class ChessPiece {
    private final String color;  // 内部状态（共享）
    public ChessPiece(String color) { this.color = color; }
    public void display(int x, int y) {  // x,y 为外部状态
        System.out.println(color + " 棋子位于 " + x + "," + y);
    }
}

class ChessFactory {
    private static final Map<String, ChessPiece> pool = new HashMap<>();
    public static ChessPiece get(String color) {
        return pool.computeIfAbsent(color, ChessPiece::new);
    }
}
```

### 3.7 代理模式（Proxy）

**意图**：为其他对象提供一种代理以控制对这个对象的访问。

**类型**：
- **远程代理**：为不同地址空间的对象提供本地代表（RPC）。
- **虚拟代理**：延迟加载开销大的对象（懒加载图片）。
- **保护代理**：控制访问权限。
- **智能引用代理**：在访问对象时附加操作（计数、缓存）。

```java
interface Image {
    void display();
}

class RealImage implements Image {
    private String file;
    public RealImage(String file) {
        this.file = file;
        loadFromDisk();  // 构造时即加载，开销大
    }
    private void loadFromDisk() { System.out.println("加载 " + file); }
    public void display() { System.out.println("显示 " + file); }
}

class ProxyImage implements Image {
    private RealImage real;
    private String file;
    public ProxyImage(String file) { this.file = file; }
    public void display() {
        if (real == null) real = new RealImage(file);  // 懒加载
        real.display();
    }
}
```

---

## 四、行为型模式

行为型模式关注"对象之间如何通信、如何分配职责"。

### 4.1 责任链模式（Chain of Responsibility）

**意图**：将请求的发送者和接收者解耦，使多个对象都有机会处理这个请求，将这些对象连成一条链，并沿着链传递请求，直到有一个对象处理它为止。

**适用场景**：审批流（组长 → 经理 → 总监）、过滤器链、Spring 中的 `HandlerInterceptor`。

```java
abstract class Handler {
    protected Handler next;
    public Handler setNext(Handler next) { this.next = next; return next; }
    abstract void handle(int amount);
}

class TeamLeader extends Handler {
    void handle(int amount) {
        if (amount <= 500) System.out.println("组长审批 " + amount);
        else if (next != null) next.handle(amount);
    }
}

class Manager extends Handler {
    void handle(int amount) {
        if (amount <= 5000) System.out.println("经理审批 " + amount);
        else if (next != null) next.handle(amount);
    }
}

class Director extends Handler {
    void handle(int amount) {
        if (amount > 5000) System.out.println("总监审批 " + amount);
    }
}
```

### 4.2 命令模式（Command）

**意图**：将一个请求封装为一个对象，从而使你可用不同的请求对客户进行参数化；对请求排队或记录日志，以及支持撤销操作。

**适用场景**：GUI 按钮、事务、宏命令、撤销/重做。

```java
interface Command {
    void execute();
    void undo();
}

class Light {
    public void on()  { System.out.println("灯开"); }
    public void off() { System.out.println("灯关"); }
}

class LightOnCommand implements Command {
    private Light light;
    public LightOnCommand(Light l) { this.light = l; }
    public void execute() { light.on(); }
    public void undo()    { light.off(); }
}
```

### 4.3 解释器模式（Interpreter）

**意图**：给定一个语言，定义它的文法的一种表示，并定义一个解释器，使用该表示来解释语言中的句子。

**适用场景**：SQL 解析、正则表达式、表达式计算引擎、规则引擎。

```java
interface Expression {
    boolean interpret(String context);
}

class TerminalExpression implements Expression {
    private String data;
    public TerminalExpression(String data) { this.data = data; }
    public boolean interpret(String context) {
        return context.contains(data);
    }
}

class OrExpression implements Expression {
    private Expression a, b;
    public OrExpression(Expression a, Expression b) { this.a = a; this.b = b; }
    public boolean interpret(String context) {
        return a.interpret(context) || b.interpret(context);
    }
}
```

### 4.4 迭代器模式（Iterator）

**意图**：提供一种方法顺序访问一个聚合对象中的各个元素，而不暴露该对象的内部表示。

**适用场景**：Java 的 `Iterator`、`for-each` 语法糖。

```java
interface Iterator {
    boolean hasNext();
    Object next();
}

interface Container {
    Iterator getIterator();
}

class NameRepository implements Container {
    private String[] names = {"张三", "李四", "王五"};
    public Iterator getIterator() {
        return new Iterator() {
            int index;
            public boolean hasNext() { return index < names.length; }
            public Object next()    { return names[index++]; }
        };
    }
}
```

### 4.5 中介者模式（Mediator）

**意图**：用一个中介对象来封装一系列的对象交互，使各对象不需要显式地相互引用，从而使其耦合松散。

**适用场景**：聊天室、机场塔台调度、MVC 中的 Controller。

```java
abstract class ChatRoom {
    abstract void sendMessage(String msg, User user);
}

class User {
    private String name;
    private ChatRoom room;
    public User(String name, ChatRoom room) { this.name = name; this.room = room; }
    public void send(String msg) { room.sendMessage(msg, this); }
    public void receive(String msg) { System.out.println(name + " 收到: " + msg); }
    public String getName() { return name; }
}

class ConcreteChatRoom extends ChatRoom {
    void sendMessage(String msg, User user) {
        System.out.println(user.getName() + " 发送: " + msg);
        // 转发给其他用户……
    }
}
```

### 4.6 备忘录模式（Memento）

**意图**：在不破坏封装性的前提下，捕获一个对象的内部状态，并在该对象之外保存这个状态，以便以后恢复。

**适用场景**：游戏存档、编辑器撤销、事务回滚。

```java
class Memento {
    private final String state;
    public Memento(String state) { this.state = state; }
    public String getState() { return state; }
}

class Originator {
    private String state;
    public void setState(String s) { this.state = s; }
    public Memento save() { return new Memento(state); }
    public void restore(Memento m) { this.state = m.getState(); }
}

class Caretaker {
    private final Stack<Memento> history = new Stack<>();
    public void push(Memento m) { history.push(m); }
    public Memento pop() { return history.pop(); }
}
```

### 4.7 观察者模式（Observer）

**意图**：定义对象间一对多的依赖关系，当一个对象的状态发生改变时，所有依赖于它的对象得到通知并自动更新。

**适用场景**：事件订阅、消息队列、Vue 的双向绑定、Spring 的 `ApplicationListener`。

```java
interface Observer {
    void update(String event);
}

class Subject {
    private final List<Observer> observers = new ArrayList<>();
    public void attach(Observer o) { observers.add(o); }
    public void detach(Observer o) { observers.remove(o); }
    public void notify(String event) {
        observers.forEach(o -> o.update(event));
    }
}
```

### 4.8 状态模式（State）

**意图**：允许一个对象在其内部状态改变时改变它的行为，对象看起来似乎修改了它的类。

**与策略模式的区别**：状态模式中状态会自动切换，策略模式由客户端选择策略。

**适用场景**：订单状态（待支付 → 已支付 → 已发货 → 已签收）、电梯运行、游戏角色状态。

```java
interface OrderState {
    void next(Order order);
    void cancel(Order order);
}

class PendingPay implements OrderState {
    public void next(Order order)  { order.setState(new Paid()); }
    public void cancel(Order order){ System.out.println("已取消"); }
}

class Paid implements OrderState {
    public void next(Order order)  { order.setState(new Shipped()); }
    public void cancel(Order order){ System.out.println("申请退款"); }
}

class Shipped implements OrderState {
    public void next(Order order)  { order.setState(new Received()); }
    public void cancel(Order order){ System.out.println("无法取消"); }
}

class Received implements OrderState {
    public void next(Order order)  { System.out.println("流程已结束"); }
    public void cancel(Order order){ System.out.println("已签收不可取消"); }
}

class Order {
    private OrderState state;
    public Order() { this.state = new PendingPay(); }
    public void setState(OrderState s) { this.state = s; }
    public void next()  { state.next(this); }
    public void cancel(){ state.cancel(this); }
}
```

### 4.9 策略模式（Strategy）

**意图**：定义一系列算法，把它们一个个封装起来，并且使它们可相互替换。使得算法可独立于使用它的客户端而变化。

**适用场景**：支付方式选择、折扣策略、排序算法切换、`Comparator`。

```java
interface PaymentStrategy {
    void pay(double amount);
}

class Alipay implements PaymentStrategy {
    public void pay(double amount) { System.out.println("支付宝支付 " + amount); }
}

class WeChatPay implements PaymentStrategy {
    public void pay(double amount) { System.out.println("微信支付 " + amount); }
}

class PaymentContext {
    private PaymentStrategy strategy;
    public void setStrategy(PaymentStrategy s) { this.strategy = s; }
    public void pay(double amount) { strategy.pay(amount); }
}
```

### 4.10 模板方法模式（Template Method）

**意图**：定义一个操作中算法的骨架，而将一些步骤延迟到子类中。模板方法使得子类可以不改变算法结构即可重定义该算法的某些特定步骤。

**适用场景**：Spring 的 `JdbcTemplate`、Servlet 的 `doGet/doPost`、构建流程固定的框架。

```java
abstract class Game {
    // 模板方法（final 防止子类修改流程）
    public final void play() {
        initialize();
        startPlay();
        endPlay();
    }
    protected abstract void initialize();
    protected abstract void startPlay();
    protected void endPlay() { System.out.println("游戏结束"); }  // 钩子方法
}

class Football extends Game {
    protected void initialize() { System.out.println("足球场准备"); }
    protected void startPlay()  { System.out.println("开球"); }
}
```

### 4.11 访问者模式（Visitor）

**意图**：表示一个作用于某对象结构中各元素的操作。使你可以在不改变各元素的类的前提下，定义作用于这些元素的新操作。

**适用场景**：编译器 AST 遍历、XML 文档解析、数据导出（导出为 JSON/XML/CSV 而不改原类）。

```java
interface Visitor {
    void visit(ElementA a);
    void visit(ElementB b);
}

interface Element {
    void accept(Visitor v);
}

class ElementA implements Element {
    public void accept(Visitor v) { v.visit(this); }
    public String featureA() { return "A 特性"; }
}

class ElementB implements Element {
    public void accept(Visitor v) { v.visit(this); }
    public String featureB() { return "B 特性"; }
}

class XmlExportVisitor implements Visitor {
    public void visit(ElementA a) { System.out.println("<A>" + a.featureA() + "</A>"); }
    public void visit(ElementB b) { System.out.println("<B>" + b.featureB() + "</B>"); }
}
```

---

## 五、设计模式对比速查表

### 5.1 创建型模式对比

| 模式 | 关键词 | 解决问题 | 典型场景 |
| --- | --- | --- | --- |
| 单例 | 唯一实例 | 全局唯一对象 | 配置、日志、连接池 |
| 工厂方法 | 子类决定 | 创建单一产品 | 多种产品类型 |
| 抽象工厂 | 产品族 | 创建一族产品 | 跨平台 UI |
| 建造者 | 分步构建 | 复杂对象可选参数多 | HTTP 请求、SQL |
| 原型 | 克隆 | 创建成本高的对象 | 深拷贝、缓存 |

### 5.2 结构型模式对比

| 模式 | 关键词 | 解决问题 | 典型场景 |
| --- | --- | --- | --- |
| 适配器 | 接口转换 | 接口不兼容 | 旧系统改造 |
| 桥接 | 抽象与实现分离 | 多维变化 | 形状×颜色 |
| 组合 | 树形结构 | 部分与整体统一 | 文件系统 |
| 装饰器 | 动态加职责 | 比继承更灵活 | Java I/O 流 |
| 外观 | 统一入口 | 简化复杂子系统 | 一键启动 |
| 享元 | 共享对象 | 大量细粒度对象 | 棋子、字符串池 |
| 代理 | 控制访问 | 延迟加载/权限 | RPC、AOP |

### 5.3 行为型模式对比

| 模式 | 关键词 | 解决问题 | 典型场景 |
| --- | --- | --- | --- |
| 责任链 | 链式传递 | 请求发送者与接收者解耦 | 审批流 |
| 命令 | 请求封装 | 撤销、队列、日志 | GUI 按钮 |
| 解释器 | 文法解释 | 自定义语言 | 规则引擎 |
| 迭代器 | 顺序访问 | 不暴露内部结构 | 集合遍历 |
| 中介者 | 集中通信 | 多对象相互引用 | 聊天室 |
| 备忘录 | 状态快照 | 撤销/恢复 | 存档 |
| 观察者 | 一对多通知 | 状态变化通知 | 事件订阅 |
| 状态 | 状态切换 | 状态多行为不同 | 订单流程 |
| 策略 | 算法切换 | 消除 if-else | 支付方式 |
| 模板方法 | 算法骨架 | 固定流程可变步骤 | 框架钩子 |
| 访问者 | 双分派 | 不改类加操作 | AST 遍历 |

---

## 六、设计模式使用建议

### 6.1 何时使用设计模式

✅ **应该使用**：
- 需求变化频繁，希望系统具备弹性
- 团队协作，需要统一的设计语言
- 框架设计、库设计，需要良好的扩展性
- 代码已经出现"坏味道"（如大量 if-else、巨型类）

❌ **避免滥用**：
- 简单脚本、一次性代码
- 需求稳定且代码清晰
- 仅为"用模式"而强行套用，反而增加复杂度

### 6.2 常见误区

1. **过度设计**：未出现需求变化即引入复杂模式。
2. **生搬硬套**：只记模式名称而不理解意图，强行套用。
3. **忽视语言特性**：某些模式在特定语言中已内置（如 Python 装饰器 ≠ GoF 装饰器模式）。
4. **混淆模式**：策略 vs 状态、工厂方法 vs 抽象工厂、代理 vs 装饰器。

### 6.3 学习路径建议

```
入门 → 单例 / 工厂 / 策略 / 观察者 / 装饰器 / 适配器 / 模板方法
   ↓
进阶 → 建造者 / 责任链 / 状态 / 外观 / 代理 / 组合
   ↓
高级 → 桥接 / 享元 / 中介者 / 备忘录 / 访问者 / 解释器 / 命令
   ↓
实践 → 结合 Spring、MyBatis、JDK 源码分析真实应用
```

---

## 七、设计模式在 JDK 与框架中的应用

| 模式 | 真实应用举例 |
| --- | --- |
| 单例 | `Runtime.getRuntime()`、`Spring` 默认单例 Bean |
| 工厂方法 | `Calendar.getInstance()`、`Spring` `BeanFactory` |
| 抽象工厂 | `UIManager.setLookAndFeel()` |
| 建造者 | `StringBuilder`、`Lombok` `@Builder`、`OkHttpClient.Builder` |
| 原型 | `Object.clone()`、`Cloneable` 接口 |
| 适配器 | `InputStreamReader`、`Arrays.asList()` |
| 桥接 | JDBC `Driver` 与 `Connection` |
| 组合 | `HashMap` 内部树、Swing 容器组件 |
| 装饰器 | `BufferedInputStream`、`DataInputStream` |
| 外观 | `SLF4J` `Logger`、`RestTemplate` |
| 享元 | `Integer.valueOf()` 缓存、`String` 常量池 |
| 代理 | JDK 动态代理、CGLIB、Spring AOP |
| 责任链 | `Filter` 链、`HandlerInterceptor` |
| 命令 | `Runnable`、`Callable` |
| 解释器 | `Pattern`（正则）、SpEL |
| 迭代器 | `Iterator`、`Stream` |
| 中介者 | `Controller`、`MessageDispatcher` |
| 备忘录 | `serializable` 序列化 |
| 观察者 | `ApplicationListener`、`PropertyChangeListener` |
| 状态 | `Thread.State`、订单状态机 |
| 策略 | `Comparator`、`Spring` `Resource` 实现 |
| 模板方法 | `JdbcTemplate`、`HttpServlet` |
| 访问者 | `FileVisitor`、ASM 字节码访问 |

---

## 八、总结

设计模式本质上是**前人经验的沉淀**，它不是万能银弹，而是工具箱中的"标准件"。掌握设计模式的关键不在于"会写多少种"，而在于：

1. **理解意图**：每种模式要解决的核心问题是什么。
2. **识别场景**：什么情境下应该套用，什么情境下应该避免。
3. **权衡取舍**：每种模式带来的好处与代价（如复杂度、性能）。
4. **融会贯通**：与 SOLID 原则、重构、架构模式结合使用。

记住 Kent Beck 的那句话：

> **"Make it work, make it right, make it fast."**（先让它能跑起来，再让它正确，最后让它快）

设计模式服务于"make it right"，但前提是先"work"。不要为了模式而模式，让代码**简单、清晰、可演进**才是最终目的。
